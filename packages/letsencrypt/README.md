# Letsencrypt docker image

```bash
bun link

bun link v1.0.25 (a8ff7be6)
Success! Registered "@trustchain/letsencrypt"

To use @trustchain/letsencrypt in a project, run:
  bun link @trustchain/letsencrypt

Or add it in dependencies in your package.json file:
  "@trustchain/letsencrypt": "link:@trustchain/letsencrypt"
```